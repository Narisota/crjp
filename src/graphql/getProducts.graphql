query apiGetProducts {
    apiGetProducts {
        product_id
        name
        desc
        price
        stock
        exp_date
        images {
            img_id
            img_url
        }
    }
}
